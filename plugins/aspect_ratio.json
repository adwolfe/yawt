{
  "version": 1,
  "name": "Aspect Ratio (Box)",
  "description": "Mean bounding-box aspect ratio (long side / short side) per worm. A straight worm has a high aspect ratio; a coiled worm approaches 1. Only single-tracking frames are included. Shown as box-and-whisker per group.",
  "aggregate": "per_worm",
  "formula": "aspect_ratio",
  "filter": "quality == Single",
  "reduce": "mean",
  "plot": {
    "type": "box",
    "y_label": "Aspect Ratio"
  }
}
