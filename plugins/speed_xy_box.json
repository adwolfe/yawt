{
  "version": 1,
  "name": "Speed from XY (Box)",
  "description": "Mean frame-to-frame centroid speed per worm, shown as box-and-whisker per group. This computes speed directly from x/y displacement and elapsed time instead of using the plugin engine's built-in smoothed speed variable.",
  "aggregate": "per_worm",
  "bindings": {
    "dx": "diff(x)",
    "dy": "diff(y)",
    "dt": "diff(t)",
    "xy_speed": "sqrt(dx*dx + dy*dy) / dt"
  },
  "formula": "xy_speed",
  "filter": "quality != Lost && dt > 0 && xy_speed > 0",
  "reduce": "mean",
  "plot": {
    "type": "box",
    "y_label": "Mean XY Speed (px/s)"
  }
}
