{
  "version": 1,
  "name": "Speed (Box)",
  "description": "Mean speed per worm, shown as box-and-whisker per group. Each data point is one worm's average smoothed speed across its tracked frames. Mirrors the built-in Speed Box plot.",
  "aggregate": "per_worm",
  "formula": "speed",
  "filter": "quality != Lost && speed > 0",
  "reduce": "mean",
  "plot": {
    "type": "box",
    "y_label": "Mean Speed (px/s)",
    "y_label_um": "Mean Speed (µm/s)"
  }
}
