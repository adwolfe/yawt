{
  "version": 1,
  "name": "Body Area (Box)",
  "description": "Mean blob area per worm across tracked frames. Only single-tracking frames with a non-zero recorded area are included. Requires re-tracking with YAWT v0.9.8+ — earlier worms.json files do not contain area data and will show no data.",
  "aggregate": "per_worm",
  "formula": "area",
  "filter": "quality == Single && area > 0",
  "reduce": "mean",
  "plot": {
    "type": "box",
    "y_label": "Body Area (px²)",
    "y_label_um": "Body Area (µm²)"
  }
}
